services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: signaliq
      POSTGRES_USER: signaliq
      POSTGRES_PASSWORD: signaliq
    ports:
      - "5433:5432"
    volumes:
      - signaliq_postgres:/var/lib/postgresql/data

volumes:
  signaliq_postgres:
